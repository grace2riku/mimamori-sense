# Issue #237: 検証と実機確認

仕様・並行性・失敗時の方針は [設計メモ](../../doc/design/issue-237.md) をマスタとする。
以下のコマンドはリポジトリルートで実行する。Python 3、ARM LLVM 21.1.1、テストには Unicorn 2.1.4 が必要。

## ローカル検証

```powershell
python scripts/issue237/tests/run_arm_tests.py
python scripts/issue237/build.py --run r1
python scripts/issue237/verify.py --run r1 --cpu1-mot "C:/firmware/CPU1.mot" --cpu1-elf "C:/firmware/CPU1.elf"
```

- テストは実CコードをARM向けにコンパイルし、RTOS・音声デバイスをモック化して実行する。実際のタスクスケジューリング、AI精度、I2C、SSI、可聴音の検証ではない。
- `ARM_CLANG` でコンパイラ、`ISSUE237_TEST_DEPS` でUnicornの配置先を指定可能。省略時の依存パスはrunner参照。
- ビルドはe2 studioの既存 `Debug/*.in` を読み、1600オブジェクトを独立した `e2studio_CPU0/Debug/issue237/<run>/` へ再コンパイルする。生成設定ファイルは変更しない。
- run名は未使用のものを指定する。入力ソースのハッシュを採取し、ビルド中に変更された場合は失敗する。
- 初回はe2 studioでCPU0をビルドして `Debug/*.in` を生成する。CPU1も対応するプロジェクトをビルドし、同一ビルドから得たMOTとELFを用意する。上例の `C:/firmware/CPU1.*` は実際の出力パスに置き換える。既存の対応する成果物ペアを明示的に指定してもよい。過去のIssueのローカル成果物を自動で探すことはない。
- 検証はCPU0のS-recordとELFのフラッシュ領域一致、チェックサム、SSI ISRラップ、指定したCPU1のMOT/ELF一致とCPU0との非重複を確認する。CPU1の入力パスと両ファイルのSHA-256を記録し、MOTを同じ出力ディレクトリへコピーする（`verify.py:91`）。リポジトリ外の入力にも対応する。これはファイルの整合性検証であり、新たに指定したCPU1の実機互換性を保証しない。
- `manifest.json` と `verification.json` の `hardware_tested: false` は、書き込み・実機確認を代行したという意味に変えない。

PR #238レビュー対応の検証: リポジトリ外へコピーしたCPU1ペアで検証成功、入力引数不足・存在しないファイル・MOT/ELF不一致を拒否すること、出力MOTを入力に再指定できること、入力パスとハッシュの記録を確認した。変更対象は検証スクリプトと文書のみで、実機確認済みのファームウェアは変更していないため、再ビルド・再書き込みは不要。

## 実機確認の準備

2026-09-26: ARMテスト4本PASS、CPU0全1600オブジェクトのビルド成功、MOT整合性検証PASS。その後ユーザーから「実機確認OKです。」の完了通知を受領。
今回の書き込みファイルは `e2studio_CPU0/Debug/issue237/r1/mimamori_sense_CPU0.mot` と同ディレクトリの `mimamori_sense_CPU1.mot`。
SHA-256はCPU0 `d0cbd867a4e5182fc40bfe497ef22fc120f22937729d7d77863fc761dde0c9cb`、CPU1 `8cb5d5e9040d0a9bb0ad72d233d21115b5e68cba3c0524e03f3e0fb4d31d2c53`。

1. 使用するCPU0/CPU1 MOTとハッシュを `verification.json` で記録する。
2. 表示確認前に [#230の最新調査記録](../../doc/design/white-screen-boot-regression.md) と [給電条件](../../doc/hardware-setup-guide/power-supply.md) を確認する。最新記録ではPOST_C待ちの暫定対策がmainへ導入済みだが、本Issueのビルドは別イメージなので、そのイメージでの実機確認が必要。
3. 表示を伴う比較は5V ACアダプタで給電し、コールドブート10回・同一セッションの対照というプロジェクト規約に従う。過去に除外済みの白画面切り分けは繰り返さない。
4. 通常監視状態で `fall threshold`、`fall status`、`alarm status`、`audio status` を記録する。音声デバイスの初期化完了、非ゼロ音量・振幅、ミュート解除を確認する。診断で無音設定にした場合は戻す（`audio mute off`、`audio volume <値>`、`alarm amp <値>`）。

## 確認項目

| 操作・条件 | 確認する結果 |
|---|---|
| 通常の人物姿勢、短い転倒疑い | 自動警報が鳴らない |
| 転倒候補が確定閾値（初期値5）まで継続 | CONFIRMEDになり緊急警報が鳴る |
| 確定後も転倒姿勢を維持 | 警報と転倒表示が継続し、時間だけで解除されない |
| 確定中に人物像が消える | 回復とは扱わず警報が継続する |
| 有効な非転倒人物像が連続5フレーム | NORMALへ戻り警報・転倒表示が解除される |
| 回復後、再び転倒候補が確定まで継続 | 待機期間なしで再確定し警報が鳴る |
| 確定中に `fall reset` | 強制解除。転倒姿勢が残れば通常の判定を経て再確定する |
| 確定中に `alarm stop` / `alarm start beep` | 使用中エラーで自動要求を上書きしない |
| 確定中に診断用 `audio stop` | 最新要求の再検査により警報が復旧する。停止直後/復旧後のstatusを保存 |
| 転倒確定前から別のテスト音が再生中 | その再生を奪わず使用中を診断。テスト音停止後に警報を開始する |

`fall status` の回復カウンタは推論フレーム単位で、コンソールの手入力だけでは4/5境界の観測を保証できない。正確な境界・不正値・開始中の解除・停止タイムアウトの制御された割り込み順は自動テストで確認し、実機では出力の対応を確認する。

`alarm status` の要求受付、再試行、失敗履歴、無音設定と、実際の可聴音を区別して記録する。正常初期化済み・バス非競合で、確定から発音／解除から無音の遅延各100ms以内を測定する。音声波形や時刻付き観測などの測定方法もログに残す。AI側追加処理のCPU実行時間1ms未満の目標は別途実機計測する。

## 実機結果の記録欄

ユーザーから「実機確認OKです。」の完了通知を受領。以下の個別ログ・実測値は未提供。動作確認の完了通知として記録し、遅延・CPU時間の数値目標や起動成功率を計測済みとは扱わない。

- 日時、基板、給電、CPU0/CPU1 MOTハッシュ:
- コールドブート結果と同一セッション対照:
- 正常→確定→維持→回復→再確定:
- 未検出時の維持、手動reset、診断干渉:
- 開始/停止遅延と測定方法:
- `fall log` / `fall status` / `alarm status` / `audio status`:
- 未確認項目:

上記の完了通知によりスキルの実機確認ゲートを満たし、コミット・PR作成へ進む。
