
# CLAUDE.md（プロジェクトメモリ）

## 概要
開発を進めるうえで遵守すべき標準ルールを定義します。

## プロジェクト構造
本リポジトリは、家庭内見守り端末(minamori-sense)ソフトウェア専用のリポジトリです。

### ドキュメントの分類

#### 1. 永続的ドキュメント（`doc/`）

ソフトウェア全体の「**何をつくるか**」「**どうつくるか**」を定義する恒久的なドキュメント。

- **product-requirements.md** - プロダクト要求定義書
  - プロダクトビジョンと目的
  - 主要な機能一覧
  - 成功の定義
  - ユーザーストーリー
  - 受け入れ条件
  - 機能要件
  - 非機能要件

## 成果物の検証ルール（コード・ドキュメント共通）

PR #166 のレビュー指摘（外部レビュアーによる2件の見落とし検出）を踏まえた再発防止ルール。
実装・設計文書・手順書・調査レポートのすべてに適用する。

1. **動作主張の裏取り必須**: 「〜すると〜になる」という動作に関する記述は、
   必ず該当コードパスを最後まで読んで検証し、根拠の `file:line` を併記する。特に確認すること:
   - 呼び出し側が**戻り値を処理しているか**（無視されていれば失敗時の挙動は変わらない）
   - 分岐が**コンパイル時（`#if`）か実行時（`if`）か**（コンパイル時分岐は実行中に切り替わらない）
   - 同期オブジェクト・リソースの**初期化場所と順序**（初期化前にアクセスされる経路がないか）
2. **置換・除外の完全性チェック**: ファイルのビルド除外・シンボル置換・バイパスを計画する場合、
   対象ファイルが定義する**全シンボル**（関数・変数）を列挙し、ビルドに残るコードからの参照を
   grep で突き合わせ、置換実装が全シンボルをカバーすることを確認する（漏れ＝リンクエラー）。
3. **ドキュメント間の整合**: 同じ情報（シンボル一覧・変更ファイル一覧・API対応表）を
   複数ドキュメントに書く場合、転記後に相互照合する。可能なら一方をマスタとし、
   他方からは参照（「詳細は○○参照」）に留めて重複記載自体を減らす。


## 実装前の設計ルール

PR #205（Issue #47）のふりかえりに基づく。同PRは26回のレビュー往復・27コミットを要し、
うち11コミット（936行）が「機構ごと破棄」となった。原因は実装の不備ではなく、
**実装前に呼び出し元・並行性・依存先の所要時間を確定していなかったこと**。

### 設計合意ゲート

実装系Issueは、コードを書く前に `doc/design/issue-<番号>.md` に設計メモを書き、
ユーザーの承認を得てから実装に入る。手順は `.claude/commands/implement-issue.md` のステップ2.5。
Issueに「設計の入力」（`.github/ISSUE_TEMPLATE/implementation.md`）が無い場合は、
実装者が洗い出して設計メモに書く。

### 要求されていない汎用性を実装しない

- 呼び出し元は**推測せず `grep` で全列挙する**。1つなら1つ向けに作る
- 「将来複数から呼ばれるかもしれない」は、実際にその呼び出し元を追加するIssueで対応する
- PR #205 では、呼び出し元がNT-Shellの1タスクだけ（`audio_alarm.c` の `usrcmd_alarm`）
  であるにもかかわらず複数タスクの同時発行を仮定し、順序付けチケット・世代カウンタ・
  放棄ウォーターマークを6コミットかけて作り、全廃棄した

### 並行性の既定形

**ブロックしうるデバイス操作は、それを所有する単一タスクに集約する。**
他タスク／ISR は「あるべき状態」を宣言してイベントを立てるだけとし、
所有タスクが現在状態との差分を reconcile する。

```
呼び出し元 : tk_dis_dsp();
               s_desired_xxx = X;      /* 状態と変更カウンタを */
               s_request_seq++;        /* 同一区間で publish する */
             tk_ena_dsp();
             tk_set_flg(...);          /* 起床通知は区間の外 */

所有タスク : tk_dis_dsp();
               want = s_desired_xxx;   /* 同じ対を同一区間で sample する */
               seq  = s_request_seq;
             tk_ena_dsp();
             デバイスが want に一致するまで start()/stop() を駆動する
```

- **複数ワードの publish と sample は、必ず同一のクリティカルセクションに入れる。**
  単純ストアを2つ並べても不可分にはならない（間にタスク切替や割り込みが入る）。
  `s_request_seq++` は read-modify-write なので、保護しなければ同時更新で失われる。
  区間を分けると、所有タスクが「新しい desired ＋ 古い seq」の組を採取して
  要求を取りこぼす。実装例: `audio_alarm.c` の `alarm_post_request()` /
  `alarm_reconcile()`（publish 側・sample 側の両方を `tk_dis_dsp()` で囲っている）。
- **`tk_dis_dsp()` が排他するのはタスクだけで、ISR は止まらない。**
  したがってこの形は**書き手がタスクだけ**であることが前提。設計メモにそれを明記し、
  ISR が書く経路があるなら排他方式を選び直す。
- **ISR と共有する要求は1ワードにパックし、アラインされた32bitストアで publish する。**
  例: `s_gen_request = (seq << ALARM_GEN_SEQ_SHIFT) | pattern`
  （`audio_alarm.c` の `alarm_gen_publish_locked()` / `alarm_fill_cb()`）。
  ストアが分割されないので、ISR は要求を丸ごと見るか全く見ないかのどちらかになり、
  消費は必ず1回になる。
- **要求の順序付けを目的とする mutex の保持区間に、ブロックしうるデバイスAPIを
  含めることを禁じる。** 保持区間に含めると、mutexの**取得順**と要求の**発行順**が
  乖離し、排他では順序を復元できない（PR #205 はこれを6回の作り直しで確認した）。
  順序付けが要るなら mutex ではなく、上記の「宣言 + 単一所有タスクの reconcile」で解く。
- **これは資源所有ロックには適用しない。**
  資源そのものを直列化するロックで、契約が「操作の完了待ちまで保持せよ」と
  定めているものは、**保持したまま呼ぶのが正しい**。本リポジトリの例:
  `i2c_bus0_lock()` / `i2c_bus0_unlock()` は各転送を submit から完了待ちまで
  囲うことが必須（`src/port/i2c_bus0.h:53-55`）であり、`i2c_bus0_suspend()` は
  カメラ診断の全体にわたって意図的に保持する（`src/port/i2c_bus0.c:191-203`）。
  これらを「禁止」と解釈してロックを外すとバスの直列化が壊れる。
- 両者の見分け方: **そのロックが守っているのは資源か、要求の順序か。**
  保持区間を短くすると資源が壊れるなら資源所有ロック（保持が契約）。
  「順序が守れなくなるだけ」なら順序付けロックであり、そもそもロックでは
  順序を守れていない（取得順 ≠ 発行順）。後者を上の既定形に置き換える。
  なお資源所有ロックを取るのも、その資源を所有する単一タスクに集約する。
- この形で不要になるのは**順序付け**（チケット・世代カウンタ・放棄ウォーターマーク）と
  **ロックタイムアウト**であって、クリティカルセクションそのものではない。
  「最新の要求が勝つ」は最後の publish がそのまま答えになる、という意味。
- 実装例: `e2studio_CPU0/src/audio_alarm.c`（設計理由は同ファイル冒頭と `s_desired_pattern` のコメント）
- 逸脱する場合は、設計メモに理由と代替案の破棄理由を書き、事前に承認を得る。

### 依存先APIの最悪所要時間を実測・裏取りする

設計を決める前に、呼び出す下位APIの実装を**最後まで読む**。特にリトライループと
タイムアウト値の掛け算に注意する（PR #205 では `audio_start()` が輻輳したIIC1バス上で
コーデックのアンミュートを再試行し、**秒オーダでブロックしうる**ことを、
機構を決めた後に知ったことが手戻りの起点になった）。


以下はCLAUDE.mdをつくってと依頼したらできた初回版。参考に残しておくが後で削除予定。

# Mimamori-Sense プロジェクト

## 概要

Renesas RA8P1マイコンを使用した組み込みシステムプロジェクト。デュアルコア構成（Cortex-M85 + Cortex-M33）でFreeRTOSとLVGLを使用。

## 開発環境

- **IDE**: e2 studio 2025-12 (25.12.0)
- **FSP**: 6.2.0 / 6.3.0
- **ツールチェーン**: LLVM Embedded Toolchain for Arm 21.1.1
- **RTOS**: FreeRTOS
- **GUIライブラリ**: LVGL

## プロジェクト構成

```
mimamori-sense/
├── e2studio/           # ソリューションプロジェクト（マルチコア統合）
├── e2studio_CPU0/      # CPU0プロジェクト (Cortex-M85 @ 1GHz)
│   ├── configuration.xml
│   ├── src/            # ユーザーソースコード
│   ├── ra_gen/         # FSP生成コード（編集禁止）
│   └── ra/fsp/         # FSPライブラリ（編集禁止）
├── e2studio_CPU1/      # CPU1プロジェクト (Cortex-M33 @ 250MHz)
├── doc/                # ドキュメント
│   ├── analysis_report/  # 解析レポート
│   ├── analysis_request/ # 解析依頼
│   └── question/         # Q&A
└── reference_projects/ # 参照プロジェクト（.gitignoreで除外）
```

## コア構成

| コア | アーキテクチャ | クロック | 役割 |
|------|---------------|---------|------|
| CPU0 | Cortex-M85 | 1GHz | メインコア、ブート担当 |
| CPU1 | Cortex-M33 | 250MHz | サブコア |

## 重要な注意事項

### 自動生成コード
- `ra_gen/` と `ra/` ディレクトリ内のファイルは**編集禁止**
- FSP設定変更は `configuration.xml` で行い、コード生成を実行する

### マルチコア開発
- **ブートシーケンス**: CPU0が先にブートし、`R_BSP_SecondaryCoreStart()`でCPU1を起動
- **クロック初期化**: CPU0のみが実施（CPU1はスキップ）
- **GPIO制御**: 各コアが制御するGPIOを分離設計する（`_RA_CORE`マクロで分岐）
- **共有リソース**: IPCセマフォで排他制御

### Threadの追加手順
1. `configuration.xml` を開く
2. Stacksタブ → New Thread
3. プロパティを設定（Symbol, Name, Stack size, Priority）
4. Generate Project Content
5. `src/<symbol>_thread_entry.c` を手動作成

### ビルド・デバッグ
- Debug出力は `.gitignore` で除外されている
- JLinkを使用してデバッグ

## ドキュメント

- 解析レポートは `doc/analysis_report/` に配置
- Q&Aは `doc/question/` に配置
- 新規ドキュメントはマークダウン形式で作成

## 参考資料

- [RA8P1製品ページ](https://www.renesas.com/en/products/ra8p1)
- [lv_port_renesas_ek_ra8p1](https://github.com/lvgl/lv_port_renesas_ek_ra8p1)
