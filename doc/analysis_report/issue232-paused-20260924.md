# #232 調査中断・記録確認（2026-09-24）

保存追記：ユーザーの依頼により、調査資料・診断スクリプトをコミット／プッシュする対象にした。
配布済み2種類の診断ZIPも `issue232-artifacts/` に無改変コピーし、SHA-256を保存。
以下の「未コミット」は中断時点の作業状態の記録。IDE自動生成設定・個人用設定は保存コミットの対象外。

ユーザー指示で調査を一時中断。TRONプログラミングコンテストの締切に向けて他機能を優先する。
追加実験・診断版作成は行わない。未解決のままであり、不具合解消とは扱わない。

## 記録確認

| 実験・観測 | 保存先（本ディレクトリ基準） |
|---|---|
| RTC保持後のAC起動で正常5/10・白5/10 | `issue-232-rtc-retention-boot.md`。個別順序・条件を下記にも保存 |
| trace-r1初回：白1回・正常1回、RTC／表示の時刻 | `issue232-trace-r1-pc/README.md`、boot-1/2の生ログ・JSON |
| 追加boottrace／描画／レジスタ比較 | `issue232-trace-r1-pc/follow-up-results/received.txt`、README・JSON |
| TP3〜TP8：正常／白の電圧 | `issue232-trace-r1-pc/lcd-power-measurements.md` |
| C31-2 VDD／R17-1 EN：正常／白 | `issue232-trace-r1-pc/u2-input-measurements.md` |
| C19-1 PG／C25-2 VREF：正常／白 | `issue232-trace-r1-pc/u2-pg-vref-measurements.md` |
| gate-r1：HOLDと再開後のPG／TP4、2起動 | `issue232-gate-r1-pc/received.txt`、README・生ログ・JSON |

会話で報告された測定値・ログは上記に保存済み。最初のAC試験は合計値だけでなく、
再開時に条件を取り違えないよう、以下にユーザー報告の個別順序と条件を補記する。

- RFPでMAIN_wait200_postc_CPU0.mot／CPU1.motの書込み内容をベリファイ済み。
- PCで時刻設定したときは白画面。その後ACアダプタ給電で確認。
- OFF→ONの間隔は30秒。
- スピーカー接続なし→あり、RTCバッテリ電源OFF→ON、時刻設定なし→ありという条件変更あり。
- 1～10回の順序：**正常、白、正常、白、正常、正常、白、正常、白、白**。
- 1回目正常では設定時刻も表示。
- 複数条件が変わっているため、RTC設定単独で再現率が変化したという因果は未確定。

## 中断時点の結論・未実施事項

gate-r1の2回ともHOLD中からPG=4.79V、TP4約2.62V。再開後も回復せず。
この診断起動ではRESETパルス／GLCDC開始前に電源異常が存在した。
1回目は白を明記、2回目の画面分類は未記載。詳細な根拠と限界はgate-r1解析を参照。
U2保護停止と整合するが、起動失敗の原因、RTCとの因果、通常版と同じ原因かは未確定。

次の候補はR_IOPORT_Openより前／後の切り分け。ただし前段ではコンソールが使えないため、
別の保留・測定手順が必要。この追加診断は設計・実装・実機試験とも未着手。
gate-r1の追加反復、待ち時間の総当たり、EN強制操作は実施していない。

## 作業環境

- 現在のブランチは既にmain。ブランチ切替は不要。
- 通常のCPU0/CPU1 srcにHEADとの差分なし。診断変更はscripts内のオーバーレイとDebug配下の別コピー。
- 設計・解析・診断スクリプトはローカルに保存済みだが、現時点では未コミット／未追跡。
  IDE設定等の既存変更も保持し、破棄・stash・コミット・pushは行っていない。
- gate-r1配布物は `e2studio_CPU0/Debug/issue232/gate-r1/LCD_GATE_r1.zip`。
- 実機は最後にgate-r1で試験したとの報告で、通常版への再書込みは確認していない。
  mainブランチであることと、実機のファームウェアが通常版であることは別。
